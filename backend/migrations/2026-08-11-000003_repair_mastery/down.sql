-- Irreversible: the pre-repair `mastery` values were inconsistent with their
-- own sub-metrics (derived data that had drifted from its source), so there is
-- no meaningful state to restore. No-op.
SELECT 1;
